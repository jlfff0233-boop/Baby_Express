# TODO

## PURPOSE

현재 최우선 작업 / 시스템 간 선행 작업 / 통합 검증만 관리한다.

* 시스템별 상세 미구현 / 미결정 항목은 System 폴더의 담당 파일 PENDING을 단일 원본으로 사용한다.
* 실제 구현 완료 상태는 [DONE.md](DONE.md) / 전체 단계는 [ROADMAP.md](ROADMAP.md)를 기준으로 확인한다.
* 세부 시스템 작업을 이 파일에 중복 나열하지 않는다.

## CURRENT_TODO

현재 우선순위

* 다음 최우선 작업은 듀라한으로 대표 확인한 직원 효과를 기준으로 나머지 7종 효과가 담당 시스템에서 정확히 한 번 적용되는지와 고용·주급·제작 할당량의 저장 복구를 검증하는 것이다.
* 다음 구현 후보는 제작 파츠 회전·좌우 반전이며, 입력 방식을 확정한 뒤 제작 안내와 Day 1 튜토리얼 문구·강조 단계를 함께 보완한다.
* `TutorialDay1Data`와 후속 가이드 데이터의 `DataMap` 명칭 정리는 기능 검증 이후 참조 마이그레이션 범위를 확인하고 진행한다.
* 현재 진행을 막지 않는 기능 확장과 추가 환경 검증은 [DEFERRED](#deferred)에 유지하고 시스템별 세부 미구현은 각 System 문서의 `PENDING`에서 관리한다.

## CROSS_SYSTEM

시스템 간 선행 결정과 공통 작업

* 정비 대상은 상점에서 최초 단계만 구매하고 상위 단계는 정비 화면에서 구매하며 장착 / 해제 / 낮은 단계 교체를 추가하지 않는다.
* 정비 Model은 영구 단계와 해금 상태만 소유하고 재고 / 용량 / 주문 / 제작 / 배송 규칙은 기존 담당 Model이 단일 원본으로 유지한다.
* 즉시 적용 효과와 다음 영업일 적용 효과를 구분해 당일 제작 할당량 / 자동 결산 / 일반 재입고 상태가 구매 중간에 바뀌지 않게 한다.
* 파츠 해금은 `PlayStateModel`, 특수 주문 해금은 평가 누적 조건을 원본으로 유지해 정비 단계와 중복 소유하지 않는다.
* 확정 데이터 에셋 작업은 Day 1 초기 자금·종족·상점 파츠 -> 주문 난이도·제목·특수 조건 -> 종족·연구 해금 -> 시설·편의·직원 -> 소모품·업적 순서로 진행한다.
* 새 파츠는 해금 당일 상점과 제작에 반영하고 이미 생성된 주문은 유지하며 다음 영업일부터 신규 주문 후보에 포함한다.
* 정비 지출 / 직접 배송비 / 직원 고용비 / 직원 주급을 상점 구매와 기타 지출에서 구분해 당일 기록 / 일일 결산 / 주간 결산 / 파산 판정에 한 번만 반영한다.
* 기본 직접 배송 1슬롯 / 직원당 동시 배송 +1 / 시설의 배송 소요일 단축을 구분하고 이미 확정된 배송 일정은 변경하지 않는다.
* 직원은 기존 배송 출발과 도착 처리를 재사용하고 주문 수락 / 파츠 선택 / 제작 판정 / 제작 완료 즉시 자동 출발을 대신하지 않는다.

## DEFERRED

현재 진행을 막지 않는 후순위

* 클라우드 저장: 기존 로컬 저장을 기준으로 클라우드 동기화 대상과 충돌 처리 방식을 확정한 뒤 연결한다.
* 로컬라이제이션: 한국어 / 영어를 지원하고 설정 화면에 언어 선택을 추가한다.
* 모바일 빌드 확인: Android / iOS 빌드에서 실행 / UI / 입력 / 저장 / 설정의 기본 흐름을 확인한다.
* 사이드 액션 추가 검증: 업적의 단계별 수령 / 전체 보상 타입 / 사이드 액션 진입 / 단일 화면 전환 / 닫기 / 재진입 갱신을 별도 확인. 가계부·카탈로그 신규 알림의 저장·해제 흐름은 연결 완료했다.
* 화면 설정 환경 검증: PC 실행 파일에서 실제 창 모드 / 해상도 변경과 확인 / 취소 / 시간 초과 복구를 확인하고 모바일 빌드에서 화면 설정 UI 제외를 확인.
* 카탈로그 슬롯 풀링 정리: 현재 `CatalogView` 내부 슬롯 재사용 구조를 명시적 오브젝트 풀 구조로 전환.
* 카탈로그 페이지 이동 검증: 파츠 데이터가 한 페이지 최대 표시 수를 초과한 뒤 이전 / 다음 페이지와 마지막 페이지 슬롯 표시를 확인.
* 카탈로그 테마 완성 보상: 테마 하나를 완성할 때마다 보상을 지급하며 완성 판정 기준 / 중복 지급 차단 상태 / 보상 종류와 수치는 구현 전에 확정.
* 제작 PC 편의 기능: 파츠 회전 / 수동 앞뒤 순서 변경 / 완전히 겹친 파츠 순환 선택.
* 제작 편의 기능: 제작 중 파츠 배치 임시 저장 / 불러오기.
* 제작 UI 다듬기: 툴팁 디자인·위치 추가 조정 / 부착 범위 시각 안내.
* 제작 밸런스: 테마 상극 실제 조합 / 감쇠율·상한·점수 수치 플레이 테스트.
* 모바일 제작 추가 조작: 회전·제거 입력을 구현한다.
* 상점 / 구매 / 인벤토리 / TopBar의 잔여 중복 방어 코드와 의미 없는 래퍼 정리.
* 특수 주문 종류별 아이콘 또는 슬롯 프레임 구분.
* 특수 주문 해금 연결: 개수 제한·목표 테마·제외 테마는 B등급 이상 평가 누적 50회 / VIP는 A등급 이상 평가 누적 50회부터 생성.
* 특수 주문 밸런스: 현재 넉넉한 최대 제작 코스트 / 개수 제한 여유 / 제외 테마 제약을 실제 제작에서 선택 부담이 생기도록 조정.
* 제외 테마 판정 변경: 제외 테마 파츠 1개 사용이 아니라 해당 테마의 최종 활성 시너지를 위반 기준으로 사용하도록 제작 판정과 표시 문구를 통일.
* 특수 테마 주문 / 카탈로그 / 직원 자동화 고도화.
* 가계부 확장: 일일 결산 모아보기 / 전체 누적 지출과 누적 이익 상단 요약.
* 카탈로그 확장: 고급 통계 / 인기도 / 제작 화면 이동.
* 업적 확장: 예약한 장식 해금 ID를 실제 장식 시스템과 연결.
* 엔딩 조건 확장: 현재는 업적 수가 적어 전체 업적의 최종 단계 달성을 엔딩 조건으로 사용하고, 업적 데이터가 충분히 추가되면 엔딩 대상 업적을 별도로 지정해 해당 업적들만 엔딩 조건으로 판정.
* 설정 확장: 저장 방식 / 추가 옵션.
* Play 설정 화면에 타이틀 이동 버튼 추가: 현재 플레이 진행 중 타이틀로 이동할 때 필요한 확인 경고 / 저장 여부 / 화면 전환 흐름을 확정한 뒤 연결.
* 오디오 확장: `AudioClipMap`과 타이틀 / 플레이 / 이벤트 BGM 및 공용 버튼·패널 효과음 기반은 구현했다. 기능별 효과음 호출 / 이벤트 BGM 실제 사용처 / 런타임 생성 UI 클릭음 / 중복 재생을 검증하고 필요한 경우 BGM 전환 페이드를 추가한다.
* 2차 제작 루프 보완: 기본 정보와 현재 정비 탭을 유지한 채 정밀 정보 단계 해금 / 장착 후 제거 불가 특수 주문 / 파츠 해금과 주문 후보 연동을 플레이 테스트 후 순차 적용. 실시간 타이머는 가장 후순위로 검토.
* 직접 배송 슬롯 확장 기반: 직접 배송 슬롯 최대치와 현재 사용량을 `DeliveryModel`의 원본 상태로 관리하고 툴팁에 `현재 / 최대` 값을 전달해 표시하며 View에 `1 / 1`을 고정하지 않도록 정리.
* Hair / Wings / Tail / Claws / Horns 실제 파츠를 추가하고 확정된 연구 단계 대상과 가격 / 조건을 데이터 에셋에 반영.
* 파산 복구 확장: 주급 부족처럼 영업 종료 처리 도중 파산한 경우 배송 도착 / 주문 기한 / 직원 고용 일수 등 이미 반영된 상태를 복구할지 재시작 전용으로 유지할지 확정하고 필요 시 원상 복구 처리 추가.

## BEFORE_UI_REWORK

UI 전면 수정 전에 처리하거나 외부 검토로 방향을 확정할 작업

1. [완료] UI 초기화와 활성화 생명주기 기준 확정: 비활성 View의 명시적 런타임 초기화 / 최초 표시 / 재진입 / 동적 슬롯과 자동 Layout 갱신 구조를 연결하고 교체 UI 통합 검증을 완료했다.
2. [부분 완료] 공용 UI 기반 정리: 루트 Canvas의 공용 `TooltipView` 검색과 재사용 구조는 구현·검증했다. 카탈로그의 명시적 오브젝트 풀 전환은 [DEFERRED](#deferred)에서 관리한다.
3. [부분 완료] UI 표시 원본 데이터 보완: 가계부 알림과 실제 연구 효과 연결은 완료했다. 카탈로그 테마 완성 보상은 [DEFERRED](#deferred)에서 관리하며 파츠 희귀도는 추가하지 않는다.
4. [완료] 오디오 표시와 재생 범위 확정: `AudioClipMap`을 음원 단일 원본으로 사용하고 타이틀 / 플레이 / 이벤트 BGM 및 공용 버튼·패널 효과음을 `AudioManager`에 연결한다. 기능별 효과음 호출과 런타임 생성 UI 검증은 `CURRENT_TODO`와 `DEFERRED`에서 이어간다.

## UI_REWORK

공용 연출과 화면 재구성 적용 순서

1. [완료] PC 공용 입력 연출과 교체 UI 연결을 완료했다. 모바일 제작 입력과 세부 입력 확장은 [DEFERRED](#deferred)에서 관리한다.
2. [완료] 공용 UI 통합 검증: Inspector 참조 / 중첩 패널 순서 / 연속 입력 / 최초 표시 / 비활성 버튼 / 클릭·패널 효과음 중복 / 슬롯 재사용 / 스크롤 시작 위치 / 화면 종료 상태를 사용자 Play Mode에서 확인했다.
3. [완료] 결산 화면 재구성: 일일 / 주간 결산을 구역별 타이틀과 개별 슬롯으로 분리하고 아이콘 / 진행 바 / 슬롯 높이 / 스크롤 시작 위치 / 날짜 전환 표시를 확인했다. 숫자 순차 집계와 구역 순차 등장은 후속 연출 범위로 유지한다.
4. [완료] 홈 화면 구성: 팩토리 배경 오브젝트 / 구름 이미지 이동과 반복 / 기본 대기 연출을 구성하고 표시를 확인했다.
5. [완료] 튜토리얼 연동: 인트로 3장면 / 공용 대화 / Day 1 핵심 루프 / Day 2~7 조건형 안내 / 첫 주간 결산·가계부·직원 가이드 / CSV 임포터 / 접근 제한 / 상태 복구를 연결하고 신규 게임 전체 실행과 단계별 저장·재접속을 확인했다.

사용자 UI 작업과 코드 작업 경계

* 사용자는 UI 소스 이미지 교체 / 정비 효과 슬롯과 특수 주문 아이콘 오브젝트 배치 / Sprite 및 컴포넌트 할당 / 탭 크기 `200 x 80`과 폰트 크기 `40` 적용을 담당한다.
* 코드는 먼저 변경 Snippet으로 검토받고, 사용자의 UI 교체가 끝난 뒤 요청을 받으면 실제 파일에 반영한다. 씬 / 프리팹 YAML은 직접 수정하지 않는다.

## UPDATE_RULE

현재 우선순위나 시스템 진행 순서가 바뀔 때 갱신하고 세부 항목은 담당 System 파일에 기록한다.
